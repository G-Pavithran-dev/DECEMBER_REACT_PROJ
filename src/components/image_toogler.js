import { useState } from "react";
import { Box, Typography,  } from "@mui/material";

const ImageToogler = () => {
    <Box>
        <Typography variant="h1" sx={{fontFamily:"cursive"}}>Imagify</Typography>
    </Box>
}

export default ImageToogler;